# Copy to .env.local for real secrets. The committed .env holds working local
# defaults, so the app runs with zero setup.
DATABASE_URL="postgresql://rhymelab:rhymelab@localhost:5432/rhymelab"
PORT="4000"
FRONTEND_ORIGIN="http://localhost:3000"
SESSION_SECRET="change-me-to-a-random-string-at-least-32-chars-long"
APP_PASSWORD="password"
